<Window x:Class="Notifier.MessageSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="未读通知"
        Width="370"
        Height="315"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <!-- 入场 -->
        <Storyboard x:Key="SlideInAnimation">
            <DoubleAnimation
                Storyboard.TargetName="RootGrid"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                From="-420" To="0" Duration="0:0:0.28">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation
                Storyboard.TargetName="RootGrid"
                Storyboard.TargetProperty="Opacity"
                From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>

        <!-- 退场 -->
        <Storyboard x:Key="SlideOutAnimation">
            <DoubleAnimation
                Storyboard.TargetName="RootGrid"
                Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                To="-420" Duration="0:0:0.28">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation
                Storyboard.TargetName="RootGrid"
                Storyboard.TargetProperty="Opacity"
                From="1" To="0" Duration="0:0:0.25"/>
        </Storyboard>
    </Window.Resources>

    <Grid x:Name="RootGrid" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform X="-420"/>
        </Grid.RenderTransform>

        <Border CornerRadius="14"
                Background="#EFF6F6F6"
                BorderBrush="#22000000"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#E2E2E2"
                        CornerRadius="14,14,0,0"
                        Padding="14,10">
                    <TextBlock Text="📢 未读通知"
                               FontWeight="SemiBold" FontSize="13"
                               Foreground="#222" VerticalAlignment="Center"/>
                </Border>

                <!-- 消息列表 -->
                <ScrollViewer Grid.Row="1"
                              Margin="12,8,12,8"
                              VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="MessageList" IsHitTestVisible="True">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border CornerRadius="8"
                                        Background="#14000000"
                                        Padding="12,8"
                                        Margin="0,4"
                                        Cursor="Hand"
                                        Tag="{Binding Title}"
                                        MouseLeftButtonDown="MessageItem_MouseLeftButtonDown">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}"
                                                   FontWeight="SemiBold" FontSize="12"
                                                   Foreground="#CC000000"
                                                   TextWrapping="Wrap"/>
                                        <ItemsControl ItemsSource="{Binding Bodies}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}"
                                                               FontSize="11"
                                                               Foreground="#88000000"
                                                               Margin="14,2,0,2"
                                                               TextWrapping="Wrap"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- 底部 -->
                <Border Grid.Row="2"
                        Background="#E2E2E2"
                        CornerRadius="0,0,14,14"
                        Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="StatusText"
                                   Grid.Column="0"
                                   FontSize="11"
                                   Foreground="#777"
                                   VerticalAlignment="Center"/>
                        <Button x:Name="ClearButton"
                                Grid.Column="1"
                                Content="清除全部"
                                Click="ClearButton_Click"
                                Background="#D32F2F"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="11"
                                Padding="10,5"
                                Cursor="Hand"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
