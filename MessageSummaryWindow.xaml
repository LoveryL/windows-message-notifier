<Window x:Class="Notifier.MessageSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="未读通知"
        Width="370"
        Height="315"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="True"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="ThemeResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="RootGrid" Opacity="0">
        <Grid.RenderTransform>
            <TranslateTransform X="-420"/>
        </Grid.RenderTransform>

        <Border CornerRadius="14"
                Background="{StaticResource LiquidGlassBackground}"
                BorderBrush="{StaticResource GlassBorderBrush}"
                BorderThickness="1"
                Effect="{StaticResource GlassShadow}"
                CacheMode="BitmapCache">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                                        Background="{StaticResource LiquidGlassBackground}"
                        CornerRadius="14,14,0,0"
                        Padding="14,10">
                    <TextBlock Text="📢 未读通知"
                               FontWeight="SemiBold" FontSize="{StaticResource FontSizeTitle}"
                               Foreground="{StaticResource LiquidGlassForeground}" VerticalAlignment="Center"/>
                </Border>

                <!-- 消息列表 -->
                <ScrollViewer Grid.Row="1"
                              Margin="12,8,12,8"
                              VerticalScrollBarVisibility="Auto"
                              CanContentScroll="True">
                    <ItemsControl x:Name="MessageList" IsHitTestVisible="True"
                                  VirtualizingStackPanel.IsVirtualizing="True"
                                  VirtualizingStackPanel.VirtualizationMode="Recycling">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border CornerRadius="8"
                                        Background="#14000000"
                                        Padding="12,8"
                                        Margin="0,4"
                                        Cursor="Hand"
                                        Tag="{Binding Title}"
                                        MouseLeftButtonDown="MessageItem_MouseLeftButtonDown">
                                    <StackPanel>
                                        <!-- 标题行 -->
                                        <TextBlock Text="{Binding Title}"
                                                   FontWeight="SemiBold" FontSize="{StaticResource FontSizeTitle}"
                                                   Foreground="#CC000000"
                                                   TextWrapping="Wrap"/>

                                        <!-- 正文条目 -->
                                        <ItemsControl ItemsSource="{Binding Bodies}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding}"
                                                               FontSize="{StaticResource FontSizeBody}"
                                                               Foreground="#88000000"
                                                               Margin="14,2,0,2"
                                                               TextWrapping="Wrap"/>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- 来源应用 -->
                                        <TextBlock Text="{Binding SourceApp}"
                                                   FontSize="{StaticResource FontSizeCaption}"
                                                   Foreground="#777777"
                                                   HorizontalAlignment="Right"
                                                   Margin="0,4,4,0"
                                                   TextTrimming="CharacterEllipsis">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <!-- 无来源时隐藏整行 -->
                                                        <DataTrigger Binding="{Binding SourceApp}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SourceApp}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- 底部 -->
                <Border Grid.Row="2"
                        Background="{StaticResource LiquidGlassBackground}"
                        CornerRadius="0,0,14,14"
                        Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="StatusText"
                                   Grid.Column="0"
                                   FontSize="{StaticResource FontSizeBody}"
                                   Foreground="{StaticResource LiquidGlassForeground}"
                                   VerticalAlignment="Center"/>
                        <Button x:Name="ClearButton"
                                Grid.Column="1"
                                Content="清除全部"
                                Click="ClearButton_Click"
                                Background="#D32F2F"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="{StaticResource FontSizeBody}"
                                Padding="10,5"
                                Cursor="Hand"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
