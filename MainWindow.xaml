<Window x:Class="Notifier.MainWindow"
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
Title="Rectangle Window"
Width="375" Height="75"
MinHeight="40" MinWidth="160"
WindowStyle="None"
AllowsTransparency="True"
Background="Transparent"
ResizeMode="NoResize"
WindowStartupLocation="Manual"
ShowInTaskbar="False"
Visibility="Hidden"
Loaded="Window_Loaded"
Closing="Window_Closing">

<Window.Resources>
    <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="ThemeResources.xaml"/>
        </ResourceDictionary.MergedDictionaries>

        <!-- Fade in/out storyboards for minimal animation changes -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25"/>
        </Storyboard>
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetName="RootGrid" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.25"/>
        </Storyboard>

    </ResourceDictionary>
</Window.Resources>

<Grid x:Name="RootGrid" Opacity="0">
    <Grid.RenderTransform>
        <TranslateTransform X="0"/>
    </Grid.RenderTransform>

    <Border CornerRadius="12"
            Background="{StaticResource LiquidGlassBackground}"
            BorderBrush="{StaticResource GlassBorderBrush}"
            BorderThickness="1"
            Padding="0"
            Margin="0"
            Effect="{StaticResource GlassShadow}"
            CacheMode="BitmapCache">
<Grid Margin="12,6,12,6">
<Grid.RowDefinitions>
<RowDefinition Height="Auto"/>
<RowDefinition Height="Auto"/>
</Grid.RowDefinitions>

<Grid Grid.Row="0" Margin="0,0,0,6">
<Grid.ColumnDefinitions>
<ColumnDefinition Width="Auto"/>
<ColumnDefinition Width="*"/>
<ColumnDefinition Width="Auto"/>
</Grid.ColumnDefinitions>
<Path Grid.Column="0" Data="{StaticResource IconBell}" Style="{StaticResource IconPathStyle}" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
<TextBlock Grid.Column="1"
Text="通知"
Foreground="{StaticResource LiquidGlassForeground}"
FontSize="{StaticResource FontSizeBody}"
FontWeight="SemiBold"
VerticalAlignment="Center"/>
<TextBlock Grid.Column="2"
x:Name="TimeStampText"
Foreground="{StaticResource LiquidGlassForeground}"
FontSize="{StaticResource FontSizeCaption}"
VerticalAlignment="Center"/>
</Grid>

<ScrollViewer Grid.Row="1"
MaxHeight="40"
VerticalScrollBarVisibility="Disabled"
HorizontalScrollBarVisibility="Disabled"
CanContentScroll="False">
<ItemsControl x:Name="MessageList"
IsHitTestVisible="False"
VirtualizingStackPanel.IsVirtualizing="True"
VirtualizingStackPanel.VirtualizationMode="Recycling">
    <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
            <VirtualizingStackPanel/>
        </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
<ItemsControl.ItemTemplate>
<DataTemplate>
<Border CornerRadius="8"
Background="#18000000"
                            Padding="8,4"
                            Margin="0,2">
<StackPanel>
<TextBlock Text="{Binding Title}"
Foreground="#DD000000"
FontSize="{StaticResource FontSizeTitle}"
FontWeight="SemiBold"
TextWrapping="Wrap"
Margin="0,0,0,5"
HorizontalAlignment="Left"/>

<ItemsControl ItemsSource="{Binding Bodies}"
IsHitTestVisible="False">
<ItemsControl.ItemTemplate>
<DataTemplate>
<TextBlock Text="{Binding}"
Foreground="#AA000000"
FontSize="{StaticResource FontSizeBody}"
TextWrapping="Wrap"
Margin="14,2,0,2"
HorizontalAlignment="Left"/>
</DataTemplate>
</ItemsControl.ItemTemplate>
</ItemsControl>
</StackPanel>
</Border>
</DataTemplate>
</ItemsControl.ItemTemplate>
</ItemsControl>
</ScrollViewer>
</Grid>
</Border>
</Grid>
</Window>